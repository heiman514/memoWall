import { Router, Request, Response } from 'express' // need to import when setup a router
// import { hashPassword } from '../hash'


export let userRoutes = Router() // set router


userRoutes.post('/login', (req:Request, res:Response)=>{
})

userRoutes.post('/signup', (req:Request, res:Response)=>{

})

userRoutes.post('/logout', (req:Request, res:Response)=>{

})




